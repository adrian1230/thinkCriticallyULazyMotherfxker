import * as React from 'react';
import {View,SafeAreaView,Text} from 'react-native';

const Map = ({ navigation }) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Map</Text>
            </View>
        </SafeAreaView>
    );
}

export default Map;