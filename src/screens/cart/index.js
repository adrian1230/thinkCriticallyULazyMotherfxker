import * as React from 'react';
import {View,SafeAreaView,Text} from 'react-native';

const Cart = ({ navigation }) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Cart</Text>
            </View>
        </SafeAreaView>
    );
}

export default Cart;