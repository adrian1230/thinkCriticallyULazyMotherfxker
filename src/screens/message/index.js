import * as React from 'react';
import {View,SafeAreaView,Text} from 'react-native';

const Chat = ({ navigation }) => {
    return (
        <SafeAreaView>
            <View>
                <Text>Chat</Text>
            </View>
        </SafeAreaView>
    );
}

export default Chat;